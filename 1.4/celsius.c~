#include <stdio.h>

int main(void) {
    int lower, upper, step;
    float celsius, fahr;

    celsius = 0;
    fahr = (celsius * 9.0/5.0 + 32);
    lower = 0;
    upper = 100;
    step = 10;

    while (step <= upper)
    {
        printf("%3d", celsius);
        printf("%6.1d", fahr);
    }
}
